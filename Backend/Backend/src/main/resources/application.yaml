spring:
  application:
    name: Backend

  datasource:
    url: jdbc:postgresql://localhost:5432/invoice
    username: 
    password: 

  mvc:
    throw-exception-if-no-handler-found: true

  web:
    resources:
      add-mappings: false

 

server:
  port: 8095
